<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= food.name %></title> 
  </head>
  <body>
    <div class="food-details">

    <h1><%= food.name %></h1>
    <% if (food.isVegan) { %>
        <p>This food is vegan!</p>
      <% } else { %>
        <p>This food is not vegan!</p>
      <% } %> 
      </div>
      <div class="food-actions">
      <form action="/foods/<%=food._id%>?_method=DELETE" method="POST" class="delete-form">
        <button class="delete-button" type="submit">Delete <%= food.name %></button>
      </form>
      <a class="edit-link" href="/foods/<%= food._id %>/edit">
        <div class="edit-text">Edit <%= food.name %>
        </div>
      </a>
      </div>
</body>
</html>
